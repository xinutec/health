import Verified

/-!
Placeholder CLI — replaced by the JSON decode interface once the library
builds. `lake exe verified_cli` is the hello-world smoke test.
-/

def main : IO Unit := do
  IO.println "verified-core: hello"
