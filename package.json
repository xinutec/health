{
  "name": "health-sync",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.15"
  },
  "scripts": {
    "test:frontend": "cd frontend && pnpm test",
    "typecheck:frontend": "cd frontend && tsc --noEmit -p tsconfig.app.json && tsc --noEmit -p tsconfig.e2e.json",
    "lint:frontend": "cd frontend && pnpm run lint",
    "verify": "scripts/dev nix run ../dev-lint#gate -- . gate-commit.json",
    "lean-check": "scripts/lean-check.sh",
    "verify:deploy": "scripts/dev nix run ../dev-lint#gate -- . gate.json"
  },
  "dependencies": {
    "mariadb": "^3.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^22.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
